//iterate from 20 to 30. For each iteration, it will check if the current number is odd or even,
for(let i=20;i<=30;i++){
    if(i%2==0){
        console.log(`${i} is even`);
    }else console.log(`${i} is odd`);
}